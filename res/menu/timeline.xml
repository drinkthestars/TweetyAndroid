<menu xmlns:android="http://schemas.android.com/apk/res/android" >
	
	<item
        android:id="@+id/menuRefresh"
        android:icon="@drawable/ic_refresh"
        android:showAsAction="ifRoom"
        android:onClick="onRefreshView"
        android:title="Refresh">
    </item>
    <item
        android:id="@+id/menuCompose"
        android:icon="@drawable/ic_compose"
        android:showAsAction="ifRoom"
        android:onClick="onComposeAction"
        android:title="Compose">
    </item>
    <item
        android:id="@+id/menuTrends"
        android:icon="@drawable/ic_trends"
        android:showAsAction="ifRoom"
        android:onClick="onTrendsView"
        android:title="Trends">
    </item>

</menu>
